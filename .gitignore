# Local development tooling/state
/.agents/
/.west/
/codex_session.sh
/skills-lock.json

# West workspace dependencies (fetched locally)
/modules/
/optional/
/zephyr/
/zmk/

# Build outputs (track only rendered keymap SVG artifact)
/build/*
!/build/artifacts/
/build/artifacts/*
!/build/artifacts/keymap.svg
/keymap.yaml
/keymap.svg
